import { parse, stringify } from "std:json";

function main() {
    let x = 42;
    let s = stringify(x);
    print("Stringified x:");
    print(s);

    let parsed = parse(s);
    print("Parsed value (dummy 42 for now):");
    print(parsed);
}
