// EXPECTED: COMPILE_ERROR
// Description: Non-void functions must return a value on every control-flow path.

function maybeNumber(flag: bool): int {
    if (flag) {
        return 1;
    }
}

function main() {}
