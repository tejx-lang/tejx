[package]
name = "tejx-rt"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
path = "core/lib.rs"

[dependencies]
libc = "0.2.183"
reqwest = { version = "0.12.12", features = ["blocking"] }
tokio = { version = "1.43.0", features = ["rt", "time", "net", "macros"] }
