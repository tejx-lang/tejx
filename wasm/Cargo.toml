[package]
name = "tejxc-wasm"
version = "0.2.0"
edition = "2024"
build = "build.rs"

[lib]
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
wat = "1.222.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(runtime_build)'] }
